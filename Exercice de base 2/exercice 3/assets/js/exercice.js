function triple (x){
    return x*3
    }
    function affiche () {
    let a = 3;
    let b = 2;
    
    alert("la fonction triple sur a : " + triple(a) );
    alert("la fonction triple sur b : " + triple(b));
    }